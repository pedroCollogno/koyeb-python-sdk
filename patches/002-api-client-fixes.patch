diff --git a/koyeb/api/api_client.py b/koyeb/api/api_client.py
index c6880fc5..c6880fc5 100644
--- a/koyeb/api/api_client.py
+++ b/koyeb/api/api_client.py
@@ -98,7 +98,13 @@ class ApiClient:
         return self
 
     def __exit__(self, exc_type, exc_value, traceback):
-        pass
+        if self.rest_client is not None and hasattr(self.rest_client, 'pool_manager'):
+            self.rest_client.pool_manager.clear()
+
+    async def close(self):
+        """Close the underlying HTTP connection pool."""
+        if self.rest_client is not None and hasattr(self.rest_client, 'pool_manager'):
+            self.rest_client.pool_manager.clear()
 
     @property
     def user_agent(self):
